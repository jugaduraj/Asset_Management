
import { type Asset, type Employee, type Log } from './types';

// Mock data is no longer used, but the arrays are kept to avoid breaking imports.
// In a real application, these would be removed and all data would be fetched from the API.
export const mockAssets: Asset[] = [];

export const mockEmployees: Employee[] = [];

export const mockLogs: Log[] = [];
